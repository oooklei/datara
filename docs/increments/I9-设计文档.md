# I9 收尾（F56~F59）· 设计文档（v3：裁定已生效，含 IA 收敛 + 画布增强 + critic 打磨）

> 状态：待用户确认后动工。v3 依据：09-19 用户三条新要求 + 前置方案裁定（1A 2A 3B 4A，见 I9-方案-信息架构与任务中心重构.md）；裁定项2/3/4（v2 遗留）以建议口径生效。
> 依据文档：01-方案与计划 §4 I9 / §6 / §8；决策记录 §十六；I9-方案-信息架构与任务中心重构.md；critic 基线 4/10（docs/increments/screenshots/i9_baseline_workbench.png）；GraphWorkbench/Palette/Inspector/routes.ts 走查。

## 0. 裁定汇总（已生效，无未决项）

| 来源 | 裁定 | 结论 |
|---|---|---|
| 前置方案裁定1 | 任务中心模式 | **1A 保持「画布直达+列表弹窗」**，画布增强在嵌入态落位 |
| 前置方案裁定2 | 任务中心提一级 | **2A**（现状已是一级，确认维持） |
| 前置方案裁定3 | 演示态模块 | **3B 折叠进一级「扩展（演示）」分组**（默认折叠）+ 页面演示标识 |
| 前置方案裁定4 | 告警与SLA | **4A 留任务中心第 5 页签**，演示态标识 |
| v2 裁定项2 | 运行监控页签换真 | 换 InstanceRunsView（/dag/instances 同组件）；/dag/runs 重定向；DagRunsView.vue 删除 |
| v2 裁定项3 | ETL 任务定位 | ETL 页签与 sync/stream 同构：候选池走 listDefinitions(type='etl')；EtlDesignView 保留 etlProfile 画布壳、删 etlTasks mock 派生；EtlListView 走 wf_definition 过滤 |
| v2 裁定项4 | I8 open() seek bug | I9 修复+单测 |
| 方案A 无争议项 | 同壳页签单入口 | 脚本三路由→1 菜单、参数四路由→1 菜单、质量报告三路由→1 菜单（原路径重定向保书签）；二级条目瘦身 45→约 25 可见项；菜单文案纪律（去重复「质量报告」、去话术） |

## 1. 目标与范围

| F# | 内容 |
|---|---|
| F56 | 前端全量适配收尾（五工作包）：**a** IA 收敛（裁定 2A/3B+无争议项）／**b** 画布交互增强 N1~N17／**c** UI/UX critic 循环打磨（≥9/10）／**d** mock→真切换＋运行监控换真＋ETL 同构＋seek bug 修复／**e** 演示态主动标识 |
| F57 | 全局回归 + 验证单：静态门 + API 冒烟 + playwright 四角色回归 + 遗留项归拢销项 |
| F58 | 规格书 V3.0 按实施态回写（build_docs.py 重生成 docx） |
| F59 | 产品发布包（web dist + api + infra + bin/install.sh 一条命令） |

### 1.1 验证门（8 门）

| 门 | 内容 | 通过标准 |
|---|---|---|
| 门1 | F56a IA 收敛 | 一级菜单：任务中心一级、真实域菜单瘦身、扩展（演示）分组默认折叠；同壳单入口且旧路径重定向可达；无重复菜单项 |
| 门2 | F56b 画布增强 | N1~N17 P1 项逐条实测（截图/操作留档）：连线提示、右键/Del 删边、面板收放持久化、Ctrl+F 搜索、类型过滤、成组折叠/解组、大纲、MiniMap/Controls 可读、fitView |
| 门3 | F56c critic 门 | 迭代循环（截图→全新上下文 critic 仅见截图→实现→复评），**退出准则 critic 独立评分 ≥9/10**（该标准不写入 critic prompt） |
| 门4 | F56d 接真核验 | 白名单外（扩展演示组）grep dataStore/seed 归零；运行监控页签真实实例；ETL 候选池走 listDefinitions；seek bug 修复+单测 |
| 门5 | F56e 演示标识 | 扩展演示组全部页面挂「演示数据」标识（截图抽检）；real 模式通知空态 |
| 门6 | F57 静态+API+回归 | vue-tsc 0 错、vitest 全绿、build 成功；ruff/py_compile（py3.11）+ smoke.py 全绿；四角色回归矩阵（含画布增强项+菜单结构项）通过 |
| 门7 | F57 遗留销项 | §3.7 清单逐条闭环或留档移交 |
| 门8 | F58+F59 | docx 重生成（宋/黑/楷/仿宋）交审；1.9 全新目录 install.sh 一条命令起全栈+冒烟 |

## 2. 现状与落点

### 2.1 画布交互现状（走查 + critic 基线 4/10）

| 能力 | 现状 | 证据 |
|---|---|---|
| 创建连线 | 已有：edit 模式端口拖拽（VueFlow @connect），校验自连/重边/分支端口，但零提示 | GraphWorkbench.vue L229-259 |
| 删除连线 | 仅隐式 edges-change remove；无右键菜单、无 hover 可供性 | L274-282 |
| 面板收放 | 无整体收放：Palette 仅类别折叠、Inspector 仅六区块折叠，两侧固定占宽 | Palette L42-74、Inspector L75-80 |
| 查找定位 | MiniMap+Controls 已挂载但样式残缺（critic 误认骨架屏）；无搜索/大纲 | L459-478 |
| 组合/过滤 | 无成组、无按类型过滤显隐 | — |
| 菜单 IA | 任务中心已一级（裁定 2A 确认）；同壳页签三处双轨（脚本 3/参数 4/质量报告 3）；「质量报告」菜单重复；演示模块与真实模块混排 | routes.ts + 截图 |

### 2.2 mock 引用全景（79 处/约 60 文件）处置三类

- **A 接真**：TaskCenterView 候选池（sync/etl/stream 同构化）、StreamDesignView（含 i8-flink 历史画布兼容）、DagListView、LineageView（lineage.py）、DagRunsView（删除）、EtlListView/EtlDesignView、DeployMonitorView servers→listRuntimeNodes、Inspector userName→useAuthStore
- **B 保留演示+标识+折叠**：扩展（演示）分组全部页面（模型/元数据/标准/指标/脚本/参数/质量/安全/开放接口/部署中心等 12 组）；WfVarPanel/Inspector 双态写法不算残留；App.vue 通知 real 空态
- **C 下线清理**：DagRunsView 删除、mock/seed 无引用段清理（dataStore/graphService(mock)/timeUtil 保留供单测与 mock 模式）、routes.ts 注释刷新

**后端不新增 API**（仅前端切换+UI 增强）。

## 3. 实现要点

### 3.1 F56a IA 收敛（菜单结构目标态）

```
总览（工作台）
任务中心（一级，画布直达 6 页签不变；运行监控页签换 InstanceRunsView）
数据集成：数据源管理 / 同步任务 / ETL任务 / 流数据处理
数据开发：数据开发IDE
数据治理：血缘分析
部署运维：运行时节点 / 集群监控
扩展（演示）（默认折叠，组头挂「演示」标识）：数仓建模 / 资产地图 / 元数据目录 / 标签管理 /
  数据标准5页（同壳后为标准管理1入口）… 指标目录 / 指标看板 / 一致性检查 / 脚本任务 / 参数配置 /
  质量（规则/任务/异常核查/质量报告/告警/评分） / 权限 / 脱敏 / 审计 / API管理 / API-Key / 调用日志 /
  部署中心 / 组件日志 / 告警管理 / 运维操作
```
- 同壳合并：脚本 3→1（ScriptHubView）、参数 4→1（ParamHubView）、质量报告 3→1（QcReportHubView）；/script/env、/param/builtin 等旧路径 redirect 保书签
- 全部路由保留（仅菜单归组），四角色菜单权限映射同步核对
- 文案纪律：去「质量报告」重复项、Beta/Phase 1/对标话术、内部 ID 直出改友好名+可复制

### 3.2 F56b 画布交互增强 N1~N17（嵌入态落位，裁定 1A）

| # | 需求 | 处置 |
|---|---|---|
| N1 | 拖入节点反馈（drop 高亮） | **P1** |
| N2 | 端口 hover 可发现、连线吸附反馈、非法目标提示 | **P1** |
| N3 | 删连线：边 hover 高亮 + 右键「删除」+ Del + toast | **P1** |
| N4 | 删节点确认（有下游提示断边影响） | **P1** |
| N5 | 框选/Shift 多选 + 批量删除 | **P1**（selectionKeyCode/panActivation） |
| N6 | 成组：框选组合/组折叠展开/整体拖动/解组/显隐（group 入 doc） | **P1** |
| N7 | 按组件类型过滤显隐（图例勾选） | **P1** |
| N8 | Ctrl+F 搜索节点名/类型→高亮+居中定位 | **P1** |
| N9 | 节点大纲（按类型分组树，点击定位） | **P1** |
| N10 | Controls 重做 + 工具栏「适配」（fitView） | **P1** |
| N11 | MiniMap 重做（nodeColor 按类型+视口框） | **P1** |
| N12 | Palette/Inspector 一键折叠+状态持久化 | **P1** |
| N13 | 画布网格背景；snap-to-grid | **P1** / P2 |
| N14 | 撤销/重做（快照栈） | P2 |
| N15 | 复制/粘贴节点 | P2 |
| N16 | 节点右键菜单（删除/重命名） | **P1 基础款** |
| N17 | 首屏构图：新画布默认 fitView+网格 | **P1** |

### 3.3 F56c UI/UX 打磨（critic 循环）

- 机制：playwright 直驱截图（http://192.168.1.9:8090，localStorage 注入 admin；脚本 Temp/gstack_i9_shot.js 已验证）→ critic 子代理（全新上下文，仅截图+固定 prompt：美学意图/工作室标准/最大差距/可供性/AI 痕迹惩罚/10 分制；紧凑具体、大胆有主见）→ 按差距实现 → 复评；**最多 3 轮一停汇报进度**；退出准则 critic 独立 ≥9/10
- 基线差距 8 条全部列入改造：右面板统计空态（节点/边数+校验状态+快捷键卡替代 emoji）、工具栏层级（保存主实心+溢出组）、色彩语义收敛（图标色=类别语义）、组件库条目降噪（单行描述/悬浮提示）、badge/话术清理（联动 3.1 文案纪律）、子页签可点样式、首屏构图（联动 N17）、MiniMap/Controls（联动 N10/N11）
- 风格基线：沿用深蓝导航+白顶栏企业级审美做提纯，不推翻重做

### 3.4 F56d mock→真切换（次序）
轻残留（Inspector/DagListView）→ TaskCenter 候选池同构化（sync/etl/stream）→ StreamDesignView 接真（i8-flink 兼容）→ LineageView 接真 → DeployMonitorView servers 切真 → 运行监控页签换 InstanceRunsView+DagRunsView 删除 → seek bug 修复（worker/stream/sources+单测）→ seed 清理。

### 3.5 F56e 演示态标识
- 路由 meta `demo: true`（扩展演示组全部路由）→ 页面标题旁「演示数据」tag，tooltip："本期范围外模块，数据为演示样例"
- 扩展（演示）分组组头挂组级标识；TaskCenter 告警与SLA 页签同规则
- App.vue 通知：real 模式空数组显示"暂无消息"
- 标识样式：低饱和描边 tag，与整体风格一致，不做红色警告

### 3.6 F57 全局回归
- 静态门：vue-tsc / vitest / build；后端 ruff+py_compile（禁 3.12 语法）
- API 冒烟：smoke.py（1.9）
- playwright 四角色矩阵（含新增项）：任务中心一级入口、扩展（演示）分组折叠+标识、画布增强（admin/dev：面板收放/连线删线/搜索/成组/过滤；analyst/viewer 只读口径复验）、运行监控真实实例、演示页标识抽检
- 截图+JSON 双证据归档 docs/increments/screenshots/i9_*.png

### 3.7 遗留项归拢（门7 销项）
I1 compose bak 删除｜I3 i3-test.py 归档 attachments｜I4 GreatDB 留档移交/文件上传 UI 核对/clean_logs 实跑随门6｜I5/I6 页面补验随门6｜I7 node-bad 已收口记录｜I8 seek bug=3.4 修复/KRaft 观察留档/i8-flink 画布兼容随 3.4。

### 3.8 F58 规格书回写
build_docs.py 修订（宋/黑/楷/仿宋）：菜单-路由表按实施态（扩展演示组/同壳重定向/新路由注记）；新增「实施状态说明」章节（已实测 vs 演示态）；重生成 docx 交审。

### 3.9 F59 发布包
```
release/datara-<ver>/：web/(dist+nginx.conf) + api/(backend+requirements) +
  infra/(compose 五容器+sql 初始化+I2 基座四脚本) + bin/install.sh（环境检查→up→健康→
  可选灌数→smoke 冒烟→输出地址与四账号）+ README（SSH 节点注册/GreatDB 说明）
```
I1 install.sh 雏形收尾（吸收 Kafka/SSH/冒烟断言）；1.9 全新空目录验证。

## 4. 海豚机制参照

采纳 DS 三条骨架纪律为设计原则（层级少而深/页面单职责/实体化菜单）；画布操作习惯对标 DS 工作流定义器（拖拽连线/右键删除/自动布局）。任务中心模式经裁定保留画布直达（不照搬 DS 列表直达）。

## 11. 1.9 实测门（八项）

门1 IA 收敛｜门2 画布增强逐条｜门3 critic ≥9/10｜门4 接真核验｜门5 演示标识｜门6 静态+API+四角色回归｜门7 遗留销项｜门8 规格书交审+发布包一条命令

## 12. 部署变更（1.9）

无新增容器/端口。前端大改（菜单结构+画布增强+全局标识）→ 部署后全量回归；发布包全新路径解包验证不影响现运行环境。清理：I1 compose bak、i3-test.py 归档、临时截图脚本用后清档。

---

> 完成定义：门1~门8 全绿 + I9-验证单.md 收口 + 决策记录 §十七 + 规格书 docx 用户审阅 + critic 终评 ≥9/10 留档。
