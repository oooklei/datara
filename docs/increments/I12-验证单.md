# I12 验证单 · 任务中心四分类工作流

> 日期：2026-09-22 起；分支 feat/ide
> 分工口径（09-18 裁定）：本地语法验证（py_compile/ruff/vue-tsc/vitest）+ 1.9 运行时实测
> 设计：[I12-设计文档.md](./I12-设计文档.md)（R1~R7）｜计划：[I12-实施计划.md](./I12-实施计划.md)

---

## P0 段 · 准备收口（2026-09-23）

### P0.1 前置回写与裁定落档
| 项 | 结论 | 提交 |
|---|---|---|
| T1 02-DAG组件清单回写 C1~C26 终态 | 以 dag.ts/executor.py/engine.py 代码事实校订；**发现 page_board 现标 code:'C24'（dag.ts:703）与 I12 C24=文件同步冲突，Task 11 须改号**（types.ts 注释同步） | 7b2ce48 |
| T2 I11-设计文档补档（流任务，以代码为准） | 四源注册化/先停再起/t_stream_job 独立生命周期/W1 表单 schema/三 use_case 均带行号 | 35ebe62 |
| 裁定 R5~R7 补录 | R5=Dependent 仅触发说明+G5 换出仓归档；R6=S3 严格按 §7.2.1 五步固定流（遵 I6 模板 B）；R7=跨工作流调度壳本期不做 | 437b6a8 |

### P0.2 D1~D7 结论表（T3，排查→修复→留痕）
| # | 项 | 结论 | 关键证据 | 运行时验证 |
|---|---|---|---|---|
| D1 | WORKER_TYPES 白名单三处同源 | 补齐 engine.py / failover.py / dag.py 三处，与 worker EXECUTORS 全集（file/http/procedure/python/shell/smoke/sql/ssh/sync）比对一致 | grep 比对，三处同源 | T12 部署后随用例 |
| D2 | 保存并发 CAS 乐观锁 | save 端点加 base_version 校验；冲突返回 WF_VERSION_CONFLICT=2005→HTTP 409（refresh 后取真实库内版本，文案精准）；base_version 缺省=兼容旧客户端；前端传 doc.version | api/wf_definition.py；graphApi.test.ts +2 用例 | 已含本地 vitest |
| D3 | 删除级联 | DELETE 前置拦截：ACTIVE_STATES（submitted/waiting_dependency/running/retry/fault_tolerance）5 态实例→409；活跃流 job→409；通过则级联删 schedule。实表名核实为 `t_workflow_instance` | api/wf_definition.py:353-393 | **1.9 现运行旧代码，运行时行为 T12 部署后复核** |
| D4 | tmp 清理覆盖 | clean_logs.sh 补清 /datara/tmp/{instance_id}/ 目录 | 脚本 bash -n 通过 | T12 部署后 |
| D5 | 变量渲染 None→空串统一 | vars_render.py + variables.py 共 5 处统一 | tools/vars_render_probe.py 17 项 PROBE_OK | 已含本地探针 |
| D6 | 流重启竞态 | 闭环成立：fail_event 清理（engine.py:362-363）+ generation+host 原子认领 + 代际守卫 | 代码走查 | T13~17 部署后随流用例回归 |
| D7 | 断电恢复 | 双层兜底成立：Redis Stream XAUTOCLAIM 未 ack 消息 + LeaderGate recover_running_instances；未发现断链 | 代码走查 | 同上 |

修复提交：da6ce39（主修）、09d467c（质量评审：CAS 条件更新/ACTIVE_STATES 拦截/env 层 None/409 留痕）、0ffe52d（CAS 冲突分支 refresh 后取真实库内版本）、9c265bb。

**归属说明**：da6ce39 捎带了工作树中遗留的前序增量改动（wf_definition.py 分类目录 CRUD、graphApi.ts 分类 API + deleteInstanceLogs、GraphWorkbench.vue 的 I11+N15+W1 功能约 350 行）。裁定：不拆分——改动自洽且已在 1.9 验证过，归属记录于此。

### P0.3 低风险 CONCERNS（不阻塞，后续观察）
1. LeaderGate 故障恢复的单次性口径（恢复动作是否可能被重复触发）。
2. 状态流 pending 消息残留场景。
3. `_join_job` 8s 超时后误删注册表的边界。

### P0.4 遗留 minor（本轮不修，留档）
| # | 项 |
|---|---|
| m2 | `_to_text` 双实现下沉 common |
| m3 | 节点参数 `or ""` 会吞 falsy 值（0/False） |
| m5 | clean_logs.sh instance_id 注入加固 |
| m6 | WF_STATE_CONFLICT 专用错误码 + 流状态常量与 master/state.py 同源化 |
| m7 | doc.version 为 undefined 时的前端用例 |
| m8 | ruff 全仓 7 处存量错误（均基线 2391789 遗留，非 T3 引入：datasource.py F821×1、seed_stream.py E501×1、seed_wf_definition.py F401+E501×3、smoke_stream_i11.py E501×1；T3 改动 8 文件单独扫描 All checks passed） |

### P0.5 存量清理基线（T4，1.9）
- 脚本：`datara-backend/tools/i12_cleanup.sh`（幂等，all/snapshot/cleanup 三模式；API 优先、直改库仅实例终止兜底且带 wf_code 白名单守卫；同步类零接触贯穿 API 与 DB 两条路径；流任务 DELETE 带 `with_def=false` 防连带删定义）。提交 55d8e21 + 质量修复 d584846（失败退出码 exit 4 / 分页 page+page_no 双参数+去重 / 注释勘误）+ 注释勘误 09783f6。
- 执行结果（零 409、零异常）：
  - before：定义 38（保留 10 / 删除 28）、实例 69 全终态（success 51 / failure 15 / kill 3）、流 job 0（此前轮已删 4 个 I11_*）
  - after：定义 10、流 job 0、实例计数不变（保留定义实例零接触）
  - 快照落盘：1.9:/mnt/lei/datara/datara-backend/tools/i12_cleanup_snapshot_{before,after}.json
- 保留集（10 条，全部 tags=['同步']；1.9 上无 demo_pipeline）：

| code | id | name |
|---|---|---|
| 62 | wf_8ea783b3 | I11_站点访问分析 |
| 53 | wf_b7262ff4 | I6-a1-source_base |
| 24 | wf_c0fba6b2 | I6-脏数据 |
| 23 | wf_49c63884 | I6-字段映射 |
| 22 | wf_203b8279 | I6-文件读端 |
| 21 | wf_45c72cc0 | I6-探测 |
| 20 | wf_f595a6ea | I6-分区隔离 |
| 19 | wf_a59b41fa | I6-标识列 |
| 18 | wf_a939dae0 | I6-并集追加 |
| 17 | wf_e7c500d2 | I6-单源基准 |

- 孤儿存量（未触碰，留档）：t_wf_schedule 剩 1 条 wf_code=2（offline，定义早已不存在）；t_wf_definition_log 剩 wf_code 42-50/55-57/60 孤儿日志（更早清理未级联所致）。

### P0.6 1.9 环境事实与探活
- 容器：Datara 栈 13 容器 **0 unhealthy**（api/logger/master/worker/alert+mysql×3 共 10 个带 healthcheck 全 healthy；datara-web/emqx/kafka/zookeeper/redis 无 healthcheck 均 Up）。
- 时钟：**无漂移**——容器 UTC+8 与宿主机 UTC-7 仅时区显示差异，UTC 基准一致（修正 T4 报告「漂移 15h」误判）。
- API 探活 5 条全 200（容器内 IP 直连，全程只读）：POST /api/v1/login；GET /api/v1/datasources（7 条）；GET /api/v1/workflow-definitions（10）；GET /api/v1/instances（total 69）；GET /api/v1/stream-jobs（0）。
- 部署差异：1.9 现运行旧版后端（api/wf_definition.py 与仓库 MD5 不一致，D2/D3 修复未部署）——**T12 统一部署后复核 DELETE 级联运行时行为**；分页 data 键实测仓库与 1.9 均返回 `list`（`items` 键仅为更早版本兜底）。

### P0.7 本地验证证据（T5 复核）
| 项 | 结果 |
|---|---|
| py_compile ×8（T3 全部改动 py） | PASS，零错误 |
| vars_render_probe | 17 项全 ok，PROBE_OK |
| ruff（T3 文件） | All checks passed（全仓存量见 m8） |
| vue-tsc --noEmit | 0 类型错误 |
| vitest run | 17 文件 / **149 用例全通过**（12.88s，含 D2 CAS 2 条新用例） |

### P0.8 提交索引
7b2ce48（T1）→ 35ebe62（T2）→ da6ce39 / 9c265bb / 09d467c / 0ffe52d（T3）→ 437b6a8（R5~R7 补录）→ 55d8e21 / d584846 / 09783f6（T4）

### P0.9 P0 门裁决
**通过**（i12-p0-gate）。进入 P1 组件横向改造。

---

## P1 段 · 组件横向改造（2026-09-22 收口）

### P1.1 任务与提交索引（T6~T12）
| 任务 | 内容 | 结果 |
|---|---|---|
| T6 | types.ts `ComponentCategory` + NodeSchema.categories；dag.ts 26 组件四类归属（C24 让出 page_board 编号） | ✅ |
| T7 | palettePinning.ts（CAT_LABEL/PIN_MAP/orderPinned 稳定排序）+ Palette 徽标 + GraphWorkbench paletteActiveTags 专属类判据 | ✅ |
| T8 | GET /datasources/{id}/topics（kafka-python 3.0.11 自适应） | ✅ |
| T9 | GET /runtime-nodes/{id}/ls?path（绝对路径 4002 / 缺省 runtime_dir / 上限 500） | ✅ |
| T10 | FieldSchema type 联合 + pick 依赖声明；pickerLogic 纯函数；Inspector 4 渲染分支；picker.test 14 用例（修 Important1 cascader 空值规整、Important2 lsDir 竞态 dirSeq+stale 双校验、Nit computed 键控） | ✅ |
| T11 | C24 file_sync / C25 assert / C26 notify 全栈（双注册：executor.py + WORKER_TYPES 四处）；engine._decide_branch 恢复守卫；9 用例（修 SFTP 5 参、staged_path 暂存、首行丢失 Critical 等 8 项） | ✅ |
| T12 | 既有组件表单改造（C11 token-insert、C17 table-picker、stream_input topic-select、stream_fuse field-select canvasX 序、C21 varHint 四级优先）+ 部署 1.9 + 浏览器实测 | ✅（④⑤深验证留 P2） |

本地门：vue-tsc 0 错、vitest **186 用例全绿**、vite build 通过、ruff（i12 文件）全过。

### P1.2 部署前修复（直接修，届时 fix(i12) 单独提交）
1. `common/sshprobe.py` 公开别名 `load_pkey = _load_pkey`（+api/runtime_node.py 改用）——T9 外部复用。
2. `api/datasource.py` GET /tmp-data/{tmp_id}/schemas F821（list_schemas_from_raw 未定义→`ok(row.schema_json or [])`）——**位于他人 hunks 内**，提交时单独剥离。

### P1.3 1.9 部署证据（Step 3）
| 项 | 结果 |
|---|---|
| 部署包 | datara_i12_backend.tgz（92 py）+ datara_i12_web.tgz + deploy_i12.sh（LF，错峰快照/markers 自检/路由探测） |
| 容器 | 四容器 healthy；**补重建 datara-master + datara-worker**（I11 脚本只重建 api/logger 的遗漏，master/worker 载 T11 执行器） |
| 自检 | check_i12.sh：EXECUTORS 11 个三容器一致；check_wt.sh：WORKER_TYPES 四处 set() 语义 WT_OK（dag.py 是 frozenset 其余 tuple，比较必须 set()——坑记录） |
| 路由探测 | 存在=401 / 不存在=404 全部符合 |
| Kafka | kafka_i12.sh：注册 i12_kafka_local（id=25，brokers=datara-kafka:9092）+ 6 真实 topic + 连通 online |
| SSH ls 三态 | 真实条目 ✅ / 相对路径 4002 ✅ / 缺省 runtime_dir ✅（smoke_i12_api.sh） |
| 数据源 | mysql id=4/9/14/15/16、file 11/12、kafka 25 |

### P1.4 部署事故与修复（留档）
| 事故 | 根因 | 修复 |
|---|---|---|
| nginx 502 | datara-web（nginx）缓存 datara-api 旧容器 IP | `docker compose restart datara-web` 代理恢复 |
| 前端双层 dist | 本地 `tar -C datara-web dist` 包内已含 dist/ 前缀，远端解包落 datara-web/dist/dist/ | 远端 `mv dist/* .`；教训：打包脚本应 `tar -C datara-web/dist .` |
| 浏览器旧 hash | WebView 缓存旧产物 | 新开 tab 强制加载 |

### P1.5 浏览器实测（Step 4，admin 登录态，画布 #63）
| # | 项 | 结果 | 证据 |
|---|---|---|---|
| ① | 组件栏分组+徽标 | ✅ 7 组全渲染：逻辑控制 10 / 数据计算 8 / 变量 1 / **数据同步 3（C17[同步,普通] C23[同步] C24[同步,普通]）** / 流处理 4 / 通用 1（C26[ETL,流,普通]）/ 模板 1；每组件 p-cat 徽标与 dag.ts categories 一致 | evaluate 全量提取；[截图01](./screenshots/i12_t12_01_canvas_open.png) |
| ② | 数据同步组置顶 | ✅ activeTags 驱动：空画布「普通」视角前三=逻辑控制/数据计算/变量（PIN_MAP.普通）；拖入 C17 后组序变为 **数据同步→逻辑控制→变量→…**（PIN_MAP['同步'] 全序吻合，专属类判据=分类仅含单一任务类+general） | evaluate 组序两次对比 |
| ③a | C17 table-picker 级联 | ✅ 选源前「请先在『读端数据源』选择数据源」+ picker disabled；选「内置源库-ec_retail」后启用；弹层列出 **ec_retail/ec_retail_bad/ec_retail_east/ec_retail_north/ec_retail_south** 真实 5 库；network `GET /datasources/9/tree` 实锤（T10+T10后端契约生效） | network 抓包 + 弹层文本 |
| ③b | C24 dir-select | ✅ 运行节点下拉（id=2 真节点 + id=3 乱码 seed，已知事实）；选节点自动 ls 缺省目录 + 点「/」ls 根目录（network `/runtime-nodes/2/ls` ×2 实锤）；**三级真实浏览 /**→/mnt（.1panel_clash/lei/lei-bak）→/mnt/lei（datara/dify/dolphinscheduler/geniePPT 等）；「选中当前目录」后必填缺失 **4 项→2 项**（剩目标数据源、目标 schema→表） | network 抓包 + 面板文本；[截图03](./screenshots/i12_t12_03_c24_dirselect.png)、[截图04](./screenshots/i12_t12_04_c24_selected.png) |
| — | 必填校验拦截 | ✅ 未配齐时点「保存」**无 PUT 发出**（network 无写请求）——requiredMissing 前置拦截生效（T10 Important1 修复的回归证据） | API 复核 graph 仍 nodes=[] |
| ④ | stream_input topic-select | ⏸ 流画布导航后 WebView 卡死（约 2.5min 未恢复）中断；**留 P2 T16 流类用例覆盖**（C18 表单本就要实测真实 6 topic） | — |
| ⑤ | 保存落库+列表 tag 归属 | ⏸ 依赖 C24/C17 必填补齐（写端 cascader 逐级点选在自动化下不稳定，键盘路径待补）；**留 P2 T14/T17 用例覆盖** | — |

### P1.6 P1 遗留（不阻塞 P2，随用例收口）
| # | 项 |
|---|---|
| L1 | 「校验」按钮点击无可见反馈（无 message/弹窗，console 无页面错误）——待查 GraphWorkbench 校验结果展示路径 |
| L2 | C24 写端 cascader 键盘导航（ArrowDown+Enter）补测 |
| L3 | 浏览器 WebView 偶发 not-ready（导航后 1~2min 不恢复需重开 tab）——测试环境问题，非产品缺陷 |
| L4 | session 窗口后端不支持（仅 tumbling/sliding），设计文档 §5.1 需回写留痕（T12 Step1 spec 建议） |

### P1.7 P1 门裁决
**通过**（i12-p1-gate）：①②③a③b 四项 UI 实测全过 + API 网络实锤；④⑤移交 P2 用例阶段（覆盖路径不变）。进入 P2 四类纵切。

---

## P2 段 · 四类纵切与 20 用例实测（待 T13~T17 后回填）

### P2.1 同步类段（T14，2026-09-23 收口）

#### A. 部署与缺陷修复（先于用例执行）
| # | 项 | 内容 | 验证 |
|---|---|---|---|
| F1 | sync.py 多 schema 选择从未生效（基线潜伏缺陷） | worker 只读 `param.readerSchemas`，前端只写 `readerSchemasText`；改为 `readerSchemasText or readerSchemas` 兜底（L74-77） | py_compile+ruff ✅，部署双容器后 S3 多 schema 同步实测生效 |
| F2 | truncate 与 autoCreate 组合缺陷（T14 实测抓到） | sync.py 原逻辑 `if truncate: TRUNCATE` 优先于 autoCreate 判定，目标表不存在时直接对不存在的表 TRUNCATE 报 1146，autoCreate+truncate 永远建不了表；改为「先查存在性→不存在则按 autoCreate 建表/报错→存在且 truncate 才 TRUNCATE」（L320-335） | py_compile+ruff ✅，部署后 S3/S5（autoCreate=true+truncate=true）实测建表成功 |
| D1 | 部署方式 | scp→1.9 /tmp→docker cp 至 datara-master、datara-worker →docker restart 双容器→healthy→grep 确认新代码 | 2026-09-23 06:48 |

#### B. 五例实测证据（批跑工具 datara-backend/tools/i12_sync_usecases.py，1.9 执行，EVIDENCE 行留痕 /tmp/i12_sync_run4.log；实例为 2026-09-23 06:49~06:55 终轮）
| 例 | 工作流 | 实例（终态 success） | 库证（SQL 双容器分查） | API 证（outputs/断言） | 页面证 |
|---|---|---|---|---|---|
| S1 全量初载 | i12_S1_ods_order_load | 20260923064905-65-8121 | src ec_retail.ods_order=250000 = dw datara_dw.ods_order=250000 | read/write_rows=250000、bad_rows=0、C25 rows≥250000 通过 | 截图 S1-instance.png |
| S2 增量定差 | i12_S2_ods_payment_incr | run1 20260923065140-66-792f / run2 20260923065152-66-d0b9 | run1 write_rows=750=窗口实测；边界=06:51:51 回写变量；run2 窗口 250=哨兵200+新注入50，write_rows=250=窗口，dw 增量 2350-2100=250，三分一致 | 变量 PUT 生效（API 实调）、run2 write_rows 与窗口一致 | 截图 S2-instance.png |
| S3 目标表反推源 | i12_S3_rev_source | run1 20260923065314-67-e040 / run2 20260923065358-67-4bdc | run1 目标 180=east100+south80（src_schema 分组）；run2 240=+north60 | outputs.schemas_included：run1 2 个→run2 3 个（含 ec_retail_north）；日志「探测到新增 schema: ec_retail_north（同名表纳入）」命中 | 截图 S3-instance.png、S3-log-autoschema.png |
| S4 文件入仓 | i12_S4_file_to_db | 20260923065412-68-4383 | dw i12_ods_order_file=500、order_no 去重=500 | rows_read=500、rows_written=500、rows_skipped=0；C25 rows[500,500]+unique 通过；日志 _filesync_ 暂存命中 | 截图 S4-instance.png、S4-log-staging.png |
| S5 多表并行 | i12_S5_multi_fork | 20260923065425-69-6cf7 | ods_cart src=dw=10000、ods_coupon 5000、ods_refund 15000（源表行数为实测快照，较创作时 9872/14750 有外部增量，C25 下限口径覆盖） | 三路 write_rows 与各自源表一致；三路 C25 通过；三路起止时间戳重叠（06:54:26~36/36/37）实证并行 | 截图 S5-dag-parallel.png |

> S2 哨兵行口径：源表存量含 200 行 `pay_time=2099-12-31`（他人 fixture，不修改），恒被增量窗口重扫，故 run2=250 属预期；断言按「窗口总数=哨兵+新注入」动态三分一致（详见 docs/use-cases/sync/I12-sync-S2 §7）。定差值法本体（边界推进、变量渲染 `>=` 语义）验证成立。
>
> 页面证截图目录：docs/use-cases/sync/shots/（另含 S0-run4-instances.png 五例实例列表总览：S1~S5 全部「成功」；S3-log-autoschema.png 可见「探测到新增 schema: ec_retail_north」+ 参与 schema 三库 + 读取 240 行；S5-dag-parallel.png 可见 fork parallelism=3 与三路 sync outputs）。

#### C. prep 留痕（1.9 执行）
- dw 预建 ods_order（SHOW CREATE TABLE 克隆 ec_retail.ods_order）；east/south/north 各建 ods_order_rev（剔除 UNIQUE、PK 偏移 0/+20万/+40万）100/80/60 行；宿主机 /mnt/lei/datara/i12files/orders.csv（500 数据行+表头）；S2 基线分批 UPDATE 历史行 pay_time=NOW()-1h。
- 容器实名（docker ps 实测）：源库 datara-mysql-src、数仓 datara-mysql-dw（dpkg-* 独立栈零接触）。

#### D. 日志核查
五例全部任务日志（容器 /datara/logs/{instance}/{task}.log，宿主 docker 卷 /mnt/lei/docker/volumes/datara_datara-logs/_data）grep ERROR/Exception/Traceback：**0 命中**；S3 autoSchema 关键字、S4 _filesync_ 暂存关键字命中。

#### E. commit 暂缓登记（裁定：等另一 agent 同步类收口后统一提交）
| 文件 | 性质 | 状态 |
|---|---|---|
| datara-backend/worker/executors/sync.py | fix(i12)：F1 readerSchemasText 兜底 + F2 truncate/autoCreate 顺序 | 已部署 1.9 实测通过，**commit 暂缓** |
| datara-backend/tools/i12_sync_usecases.py | feat(i12)：S1~S5 批量编排工具（prep+建流+跑批+三证取证） | 1.9 实测通过，**commit 暂缓** |
| docs/use-cases/README.md、docs/use-cases/sync/*.md（5 份） | docs(i12)：统一模板+S1~S5 用例文档 | 成文+实测回填完成，**commit 暂缓** |

#### F. 同步类门裁决
**通过**（i12-sync-gate）：五例三证齐全（库证 SQL 实查 + API outputs 实调 + 页面截图）+ 日志零异常 + F2 缺陷修复回归通过 + 零接触约定遵守（仅 i12_ 前缀工作流，未动他人同步类工作流与 dpkg 栈）。

### P2.2 ETL 类段（T15，2026-09-23 收口）

#### A. 部署与缺陷修复（先于用例执行）
| # | 项 | 内容 | 验证 |
|---|---|---|---|
| — | 平台零改动 | E1~E5 全部组件（C11 SQL / C25 校验 / C1/C2 / fork+merge）均为 P1 T11/T12 已部署能力，**无新增后端代码、无部署** | 1.9 现役代码直接跑通五例 |
| T1 | 批跑工具断言缺陷（首轮抓到） | `i12_etl_usecases.py` 首轮汇总 `E5: FAIL`，但三段式实测行为正确（run1 success / run2 failure / run3 success）——L695 闭环断言按错误实例取数误伤；修复为按 run 序逐实例断言 | 修复后重跑 `etl_e5_run.log` → **E5: PASS**（B 表实例为终轮） |

#### B. 五例实测证据（批跑工具 datara-backend/tools/i12_etl_usecases.py，1.9 执行；EVIDENCE 行留痕 datara-backend/tools/etl_run.log + etl_e5_run.log；实例为 2026-09-23 11:15~11:25 终轮）
| 例 | 工作流 | 实例（终态） | 库证（数仓容器 SQL 对账） | API 证（outputs/断言） | 页面证 |
|---|---|---|---|---|---|
| E1 清洗入DWD | i12_E1_ods_to_dwd_clean（code70） | 20260923111549-70-02f6 success | dwd_order_clean=249632=源去重口径 249632；order_no 重复组 0；amount<0 行 0 | rows_match 249632 vs 249632；assert_success=True（C25 3 规则全过） | 截图 E1-instance.png |
| E2 DWS聚合 | i12_E2_dws_daily_agg（code71） | 20260923111642-71-30b8 success | dws_order_stat_day=90 行 / dws_order_stat_status=540 行（两分支产物均落库） | day_sum/status_sum 均 249632 vs 249632；both_branches_ran 两分支都跑；assert_success=True | 截图 E2-instance.png |
| E3 ELT文件 | i12_E3_elt_file_raw（code72） | 20260923111654-72-20bc success | i12_ods_order_raw=500（raw 原样入仓）= orders.csv 500 数据行；dwd_order_file=500（类型规整 500/500，parse 失败 0） | raw_rows=500、fs_rows_written=500、rows_skipped=0；assert_success=True | 截图 E3-instance.png |
| E4 文件维表JOIN | i12_E4_file_join_dim（code73） | 20260923111718-73-a270 success | 维表临时表 user_region=59451（变量快照已解析）；dwd_order_region=250000=ods_order 全关联；region 空值行 0（full cover） | file_rows_count=59451、tmp_name=user_region；join_rows_match 250000 vs 250000、region_full_cover=0；assert_success=True | 截图 E4-instance.png |
| E5 带闸门分层 | i12_E5_gated_pipeline（code74） | run1 20260923112250-74-7642 success / run2 20260923112350-74-1c11 failure / run3 20260923112446-74-b94e success | run1 dwd_order_gate=4995、dws_gate_day=2；run2 源清空后 dwd=0（重建自空源）而 **dws 保持 2 未动（断流实证）**；run3 恢复源后 dwd=4995 vs 4995、dws=2 vs 2 | run1/run3 assert_ok=true；**run2 failure**、闸门一 outputs.failed=['行数 0 不在区间 [1, ∞]']、分层二/闸门二/结束全部 skip | 截图 E5-fail-instance.png + E5-success-instance.png |

> 页面证截图目录：docs/use-cases/etl/shots/。E-runs-list.png 列表总览（20 行：E1~E5 全部实例，含 E5 失败 run，状态列齐全）；E1~E5-instance.png 为各实例详情抽屉（DAG 染色 + 任务表逐节点状态/attempt/时长 + 闸门日志关键字，抽屉文本已随截图一并导出留痕）。

#### C. prep 留痕（1.9 执行）
- E1/E2/E4/E5 共用 dw.ods_order=250000 行（S1 产物）；E3 文件 /mnt/lei/datara/i12files/orders.csv（501 行=表头+500 数据）；E4 维表 /mnt/lei/datara/files/i12_etl_user_region.csv（59451 行）；E5 自有源表 i12_o_order_src（克隆 ods_order 结构、5000 行，下游表初清，TRUNCATE 篡改只动自有表）。
- 容器实名（docker ps 实测）：数仓 datara-mysql-dw（与同步类一致）。

#### D. 日志核查
五例全部任务日志 grep ERROR/Exception/Traceback：**0 命中**；E3 `_filesync_` 暂存 SFT 链路关键字、E4 变量快照 `${tmp.user_region}` 解析、E5 run2「不通过: 行数 0 不在区间 [1, ∞)」关键字命中（预期失败留痕）。

#### E. commit
| 文件 | 性质 | 状态 |
|---|---|---|
| datara-backend/tools/i12_etl_usecases.py | feat(i12)：E1~E5 批跑工具（prep+建流+跑批+三证取证，含 E5 L695 断言修复） | 本次提交 |
| docs/use-cases/etl/*.md（5 份）+ shots/（7 png） | docs(i12)：E1~E5 用例文档 + 页面证截图 | 本次提交 |
| docs/increments/I12-验证单.md | docs(i12)：P2.2 ETL 段回填 | 本次提交 |

#### F. ETL 类门裁决
**通过**（i12-etl-gate）：五例三证齐全（库证 SQL 对账 + API outputs/断言实调 + 页面证截图与抽屉文本）+ 日志零异常 + E5 断流闭环实证（run2 failure + 下游 skip + DWS 未动）+ 零接触约定遵守（仅 i12_ 前缀工作流/表，未动共享 fixture 与 dpkg 栈）。

### P2.3 流类段（T16，2026-09-24 文档补齐）

#### A. 文档与工具状态
| # | 项 | 状态 |
|---|---|---|
| — | 流类用例文档 5 份（F1~F5） | ✅ 已创作（docs/use-cases/stream/），章节完整（意图/DAG/参数表/验收三证口径） |
| — | 批跑工具 i12_stream_usecases.py | ✅ 已提交（23bb3b8，795 行，建流+灌数+EVIDENCE 取证） |
| — | F4 C26 尾联 i12_F4_notify | ✅ 文档含双实例（成功/故意失败）notify 规格 |
| — | 页面证截图 shots/ | ❌ 待 1.9 运行时取证（5 处引用悬空：F1~F5 各一帧） |

#### B. 五例文档参数锚点（待 1.9 实测回填实例 ID 与证据值）
| 例 | 工作流 | 形态 | 数据出口 |
|---|---|---|---|
| F1 单源窗口API | i12_F1_kafka_window_api | kafka→window(10s)→api | API 通道 win 行 |
| F2 双源union看板 | i12_F2_kafka2_union_board | kafka×2→union→api+board | API+page_board |
| F3 join维表 | i12_F3_kafka_join_dim | kafka×2→join(goods_id,left,60s)→api | API 通道命中行 |
| F4 http阈值 | i12_F4_http_threshold | http→filter→window→api + C26 尾联 | API+webhook |
| F5 窗口落仓 | i12_F5_kafka_table_agg | kafka→window→TableSink(i12_stream_agg) | 数仓表+手动下游 SQL |

#### C. 流类门裁决
**文档通过，运行时证据待补**（i12-stream-gate-pending）：五份用例文档参数表完整、验收口径明确；页面证截图与实例 ID 待 1.9 部署后随流用例执行补全（覆盖路径：批跑工具 EVIDENCE 行 + playwright 5 帧截图）。

### P2.4 普通类段（T17，2026-09-24 补齐）

#### A. 文档与工具状态（本次补齐）
| # | 项 | 状态 |
|---|---|---|
| — | 普通类用例文档 5 份（G1~G5） | ✅ 已创作（docs/use-cases/general/，本次新增） |
| — | 批跑工具 i12_general_usecases.py | ✅ 已创建（本次新增，建流+跑批+EVIDENCE 取证，ruff All checks passed） |
| — | 页面证截图 shots/ | ❌ 待 1.9 运行时取证 |

#### B. 五例文档参数锚点（待 1.9 实测回填）
| 例 | 工作流 | 形态 | 数据出口 |
|---|---|---|---|
| G1 shell巡检 | i12_G1_shell_health_check | shell→delay(60s)→C26(log) | 任务日志 |
| G2 python清洗 | i12_G2_python_clean_file | python(csv清洗)→C22(预览) | 清洗后 CSV |
| G3 http落库通知 | i12_G3_http_to_db_notify | http→sql→C26(webhook) | 数仓表+webhook |
| G4 ssh多节点并行 | i12_G4_ssh_fork_parallel | fork→ssh×2→join | 并行时间戳 |
| G5 出仓归档 | i12_G5_data_export_archive | sql→python(CSV导出)→C26(webhook) | 共享目录 CSV |

#### C. 普通类门裁决
**文档通过，运行时证据待补**（i12-general-gate-pending）：五份用例文档按统一模板创作完成（意图/DAG/逐节点参数表/验收三证口径/日志查看）；批跑工具通过 py_compile + ruff。页面证截图与实例 ID 待 1.9 部署后执行补全。

